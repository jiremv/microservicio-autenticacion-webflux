package com.pragma.domain.model;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.*;
import java.util.Set;
import java.util.UUID;

class UserTest {
/*    @Test
    void create_user_requires_email() {
        var id = new UserId(UUID.randomUUID());
        assertThatThrownBy(() -> new User(id, null, "Alice", Set.of(RoleName.USER)))
                .isInstanceOf(IllegalArgumentException.class);
    }

    @Test
    void user_has_roles_invariant() {
        var u = new User(new UserId(UUID.randomUUID()), new Email("a@b.com"), "Alice", Set.of(RoleName.USER));
        assertThat(u.roles()).contains(RoleName.USER);
    }*/
}
